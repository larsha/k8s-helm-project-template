apiVersion: v1
kind: Service
metadata:
  name: asda-nginx-service
  namespace: egm-development
spec:
  type: LoadBalancer
  ports:
  - port: 6060
    targetPort: 80
    protocol: TCP
  selector:
    app: asda-nginx-deployment
