apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: asda-deployment
  namespace: egm-development
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: asda-deployment
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - asda-deployment
              topologyKey: kubernetes.io/hostname
      containers:
      - name: asda
        image: docker.for.mac.localhost:5000/egm-development/asda-deployment:latest
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: asda-configmap
        ports:
        - containerPort: 3000